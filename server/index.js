// const express = require('express') bcuz the new-version we can do import things
import express from "express";
import bodyParser from "body-parser";
import mongoose from "mongoose";
import cors from 'cors';
